charlotte: Alright…[pause=0.5] here we go...[pause=0.5] I just have to insert the code…
label insert_diary_code
[text_input text="Insert the code." var="inserted_diary_code" placeholder="0000"]
if {inserted_diary_code} != {diary_code}:
	jump insert_diary_code
charlotte: Finally!
[signal arg="show_diary_page"]
